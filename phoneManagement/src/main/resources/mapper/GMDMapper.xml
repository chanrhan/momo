<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.momo.mapper.GMDMapper">
    <select id="getDevice">
        select device_id,
        device_nm,
        device_cd
        from    tb_device
        where
        <if test="keyword != null and !keyword.equals('')">
            (
            device_cd like '%${keyword}%' or
            device_nm like '%${keyword}%'
            ) and
        </if>
        1=1
        <!--        <if test="order != null and !order.equals('')">-->
        <!--            order by ${order} ${asc}-->
        <!--        </if>-->
        limit 20
    </select>

    <select id="getSecondDevice">
        select sec_device_id,
               sec_device_nm,
               sec_device_cd
        from    tb_second_device
        where
        <if test="keyword != null and !keyword.equals('')">
            (
            sec_device_cd like '%${keyword}%' or
            sec_device_nm like '%${keyword}%'
            ) and
        </if>
        1=1
        <!--        <if test="order != null and !order.equals('')">-->
        <!--            order by ${order} ${asc}-->
        <!--        </if>-->
        limit 20
    </select>

    <select id="getExtraService" parameterType="com.momo.common.vo.GMDVO">
        select exsvc_id,
               exsvc_nm
        from    tb_extra_service
        where
        <if test="keyword != null and !keyword.equals('')">
            (
                exsvc_nm like '%${keyword}%'
            ) and
        </if>
        1=1
<!--        <if test="order != null and !order.equals('')">-->
<!--            order by ${order} ${asc}-->
<!--        </if>-->
        limit 20
    </select>

    <select id="getInternetPlan">
        select internet_plan_id,
               internet_plan_nm
        from    tb_internet_plan
        where
        <if test="keyword != null and !keyword.equals('')">
            (
            internet_plan_nm like '%${keyword}%'
            ) and
        </if>
        1=1
        <!--        <if test="order != null and !order.equals('')">-->
        <!--            order by ${order} ${asc}-->
        <!--        </if>-->
        limit 20
    </select>

    <select id="getTvPlan">
        select tv_plan_id,
                tv_plan_nm
        from    tb_tv_plan
        where
        <if test="keyword != null and !keyword.equals('')">
            (
            tv_plan_nm like '%${keyword}%'
            ) and
        </if>
        1=1
        <!--        <if test="order != null and !order.equals('')">-->
        <!--            order by ${order} ${asc}-->
        <!--        </if>-->
        limit 20
    </select>

    <select id="getCtPlan">
        select ct_plan_id,
                ct_plan_nm
        from    tb_ct_plan
        where
        <if test="keyword != null and !keyword.equals('')">
            (
            ct_plan_nm like '%${keyword}%'
            ) and
        </if>
        1=1
        <!--        <if test="order != null and !order.equals('')">-->
        <!--            order by ${order} ${asc}-->
        <!--        </if>-->
        limit 20
    </select>


</mapper>