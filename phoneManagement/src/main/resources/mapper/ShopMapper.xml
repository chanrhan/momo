<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.momo.mapper.ShopCommonMapper">
    <select id="getBpNoByShopId">
        select
            cp.bp_no
        from
            tb_shop sh
        left outer join tb_corp cp on sh.corp_id=cp.corp_id
        where
            shop_id=#{id}
    </select>

<!--    Shop-->
    <insert id="insertShop" parameterType="com.momo.vo.UserCommonVO">
        insert into
            tb_shop
            (shop_id, corp_id, shop_nm, shop_addr, shop_tel)
        values
            (#{shopId},#{corpId},#{shopNm},#{shopAddr},#{shopTel})
    </insert>

    <update id="updateShop" parameterType="com.momo.vo.UserCommonVO">
        update
            tb_shop
        set
        <if test='shopNm != null and !shopNm.equals("")'>
            shop_nm=#{shopNm},
        </if>
        <if test='corpId != null'>
            corp_id=#{corpId},
        </if>
        <if test='shopTel != null and !shopTel.equals("")'>
            shop_tel=#{shopTel},
        </if>
        <if test='sendTel != null and !sendTel.equals("")'>
            send_tel=#{sendTel},
        </if>
        <if test='shopAddr != null and !shopAddr.equals("")'>
            shop_addr=#{shopAddr},
        </if>
        shop_id=#{shopId}
        where
            shop_id=#{shopId}
    </update>

    <delete id="deleteShop">
        delete from
            tb_shop
        where
            shop_id=#{id}
    </delete>

    <select id="getMaxShopId">
        select
            max(shop_id)
        from
            tb_shop
    </select>

    <select id="searchShop" parameterType="com.momo.vo.SearchVO">
        with branch as(
            select
                c.reps_id as reps_id,
                c.bp_ko_nm as bp_ko_nm,
                c.bp_en_nm as bp_en_nm,
                c.corp_nm as corp_nm,
                c.bp_no as bp_no,
                s.*
            from
                tb_shop as s
            left outer join tb_corp c on c.corp_id = s.corp_id
            )

        select
            *
        from
            branch
        where
            ${andSelect} and
            ${orSearch}
        ${prop}
    </select>

    <select id="selectShop" parameterType="com.momo.vo.ShopCommonVO">
        with tb as (
            select
                sh.*,
                cp.bp_no as bp_no,
                ac.name as reps_nm
            from
                tb_shop sh
            left outer join tb_corp cp on cp.corp_id=sh.corp_id
            left outer join tb_account ac on ac.id=cp.reps_id
        )

        select
            *
        from
            tb
        where
        <if test='corpId != null and !corpId.equals("")'>
            corp_id=#{corpId} and
        </if>
        <if test='shopId != null'>
            shop_id=#{shopId} and
        </if>
        <if test='bpNo != null and !bpNo.equals("")'>
            bp_no=#{bpNo} and
        </if>
        <if test='shopNm != null and !shopNm.equals("")'>
            shop_nm=#{shopNm} and
        </if>
        <if test='shopAddr != null and !shopAddr.equals("")'>
            shop_addr=#{shopAddr} and
        </if>
        <if test='shopTel != null and !shopTel.equals("")'>
            shop_tel=#{shopTel} and
        </if>
        <if test='regiDt != null and !regiDt.equals("")'>
            regi_dt=#{regiDt} and
        </if>
            1 = 1
        <if test='order != null and !order.equals("")'>
            order by
            ${order} ${asc}
        </if>
        <if test='limit != null and offset != null'>
            limit ${offset} ${limit}
        </if>
    </select>

<!--    Corperation-->


    <insert id="insertCorp" parameterType="com.momo.vo.ShopCommonVO">
        insert into
            tb_corp
            (corp_id,reps_id, bp_no, bp_ko_nm, bp_en_nm, corp_nm, corp_tel, start_dt)
        values
            (#{corpId},#{repsId},#{bpNo},#{bpKoNm},#{bpEnNm},#{corpNm},#{corpTel},#{startDt})
    </insert>

    <update id="updateCorp" parameterType="com.momo.vo.ShopCommonVO">
        update
            tb_corp
        set
        <if test='bpKoNm != null and !bpKoNm.equals("")'>
            bp_ko_nm=#{bpKoNm},
        </if>

        <if test='bpEnNm != null and !bpEnNm.equals("")'>
            bp_en_nm=#{bpEnNm},
        </if>

        <if test='corpNm != null and !corpNm.equals("")'>
            corp_nm=#{corpNm},
        </if>

        <if test='corpTel != null and !corpTel.equals("")'>
            corp_tel=#{corpTel},
        </if>

        <if test='startDt != null and !startDt.equals("")'>
            start_dt=#{startDt},
        </if>
        reps_id=#{repsId}
        where
            corp_id=#{corpId}
    </update>

    <update id="updateCorpPoint">
        update
            tb_corp
        set
            corp_pt=corp_pt+(#{amount})
        where
            corp_id=#{id}
    </update>

    <delete id="deleteCorp">
        delete from
            tb_corp
        where
            corp_id=#{id}
    </delete>

    <select id="getMaxCorpId">
        select
            max(corp_id)
        from
            tb_corp
    </select>

    <select id="selectCorp" parameterType="com.momo.vo.ShopCommonVO">
        select
            *
        from
            tb_corp
        where
        <if test='corpId != null'>
            corp_id=#{corpId} and
        </if>
        <if test='repsId != null and !repsId.equals("")'>
            reps_id=#{repsId} and
        </if>
        <if test='bpNo != null and !bpNo.equals("")'>
            bp_no=#{bpNo} and
        </if>
        <if test='bpKoNm != null and !bpKoNm.equals("")'>
            bp_ko_nm=#{bpKoNm} and
        </if>
        <if test='bpEnNm != null and !bpEnNm.equals("")'>
            bp_en_nm=#{bpEnNm} and
        </if>
        <if test='corpNm != null and !corpNm.equals("")'>
            corp_nm=#{corpNm} and
        </if>
        <if test='corpTel != null and !corpTel.equals("")'>
            corp_tel=#{corpTel} and
        </if>
        <if test='startDt != null and !startDt.equals("")'>
            start_dt=#{startDt} and
        </if>
        1 = 1
        <if test='order != null and !order.equals("")'>
            order by
            ${order} ${asc}
        </if>
        <if test='limit != null and offset != null'>
            limit ${offset} ${limit}
        </if>
    </select>

    <select id="searchCorp" parameterType="com.momo.vo.SearchVO">
        select
            *
        from
            tb_corp
        where
            ${andSelect} and
            ${orSearch}
                ${prop}
    </select>

    <select id="getCorpPoint">
        select
            corp_pt
        from
            tb_corp
        where
            corp_id=#{id}
    </select>


</mapper>